import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        margin: 5,
        height: 76,
        width: 76,
        borderColor: "#ae22e0",
        borderRadius: 50,
        borderWidth: 3,
    },

    image: {
        borderRadius: 50,
        borderWidth: 3,
        borderColor: "#ffffff",
    }
});

export default styles;